This repository was used in the past for serving the www.jgrapht.org website.  We changed to serving it from the main JGraphT repository, so this repository is no longer active and is preserved in archive mode only as history.
